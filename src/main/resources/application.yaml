ktor:
    application:
        modules:
            - com.example.ApplicationKt.module
    deployment:
        port: 8080
        watch:
            - classes
            - resources
    development: true
    line:
        channel:
            secret: "default_secret"
            token: "default_token"
